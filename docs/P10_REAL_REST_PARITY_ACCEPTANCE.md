# P10 real REST parity acceptance

This acceptance note tracks the final live comparison after the REST geometry correction is merged.

Acceptance scope is fixed to Figma file `01SIsqGVDm32KsaZnxHPR9`, frame `3434:8258` (`Desktop — Original`). The retained plugin baseline is score `75` / status `REVIEW`, with `11` active backlog items and `7` IMPROVEMENT items.

The corrected CLI rerun must demonstrate equivalent root statistics/findings and equivalent section-recipe/backlog semantics on that exact real scope. Static tests and fixture-only agreement are supporting evidence, not a substitute for the real rerun.
