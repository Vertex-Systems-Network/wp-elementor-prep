# P10 real REST parity guardrails

- Use the same real Figma file and frame as the retained plugin baseline.
- Keep credentials local; never serialize tokens into evidence.
- Treat REST/plugin semantic disagreement as a failure until explained and corrected.
- Do not promote P12 acceptance from static CI alone.
