# P10 parity fix scope

This change is intentionally limited to REST-to-AuditNode geometry normalization and regression coverage. It does not alter scoring, backlog generation, mutation behavior, plugin runtime behavior, release UI, or Community metadata.
