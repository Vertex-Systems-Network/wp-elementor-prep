# Project State

Last updated: 2026-09-16

## Product direction

WP Builders Prepare is a deterministic Figma audit/safe-prep platform evolving toward validated multi-target build output while preserving offline/fail-closed authority boundaries.

`config/runtime-artifacts.json` is runtime artifact registry schema v3 and remains the machine-readable runtime artifact authority.

## Current repository main

Current verified main after P15 mapping-readiness merge:

`d09f5430831394c21033585354fe7daceaba78d4`

Latest accepted commercial-V1 P15 slice:

- issue #473 / PR #474 — deterministic compatibility coverage + categorical mapping readiness;
- exact PR head `438bab50e3683580b15bc9cd25a48eb66a37ede3`;
- CI #1262 PASS;
- P12 Final Release Artifact #573 PASS;
- P12 Offline Acceptance #617 PASS;
- merge `d09f5430831394c21033585354fe7daceaba78d4`.

## Persistent issue queue

- #84 — P12 final integrated validation/release exit; retained at 80%;
- #119 — P13-P27 commercial/multi-target roadmap owner;
- #159 — genuine Figma Desktop P13 Build-Ready runtime/parity acceptance dependency;
- #182 — P27 final production-release/evidence gate;
- #287 — repository-admin branch protection/ruleset hardening residual;
- #475 — AI-native release-train cadence / roadmap-truth speed cleanup.

## AI-native execution state

Repository development remains issue-first, PR-second, R0/R1-aware and exact-head gated.

The active execution policy is being tightened around focused release trains:

- one acceptance objective per focused issue/branch/PR;
- related implementation/tests/UI/status-sync commits may stay in one train;
- focused typecheck/tests/builds provide fast branch feedback;
- full documented CI/release/offline checks remain mandatory on the exact integration/PR head before merge;
- canonical status/docs synchronize once when truth changes rather than after every micro-commit;
- parallel agents use non-overlapping file ownership and one explicit integrator for shared hotspots;
- no synthetic overall project percentage.

This process change does not change product/runtime/target authority.

## P12 state

P12 remains **IN PROGRESS / 80%**.

Historical publishing authority remains Final Release Artifact #20 from source `5f12b1d28146d5c2af815cc9f83eb30431dce4b5`, plugin ID `1680034649341961379`.

Later P13-P16 development does not replace this publishing candidate by implication. Remaining live package/publisher/account/2FA/final-exit evidence stays under #84/#182.

## P13 state

P13 is **IMPLEMENTATION COMPLETE / RUNTIME ACCEPTANCE PENDING** with `100% impl`.

#159 still requires genuine Figma Desktop runtime/parity evidence and a separate internal acceptance review.

## P14 state

P14 remains **CORE IMPLEMENTATION IN PROGRESS / RUNTIME UNWIRED** with `N/A` progress.

`PRODUCTION_P14_SAFE_RECIPE_REGISTRY` remains empty. Read-only preview/review evidence does not create production mutation authority. #159 remains required before real P14 mutation exposure.

## P15 state

P15 remains **CORE FOUNDATION IN PROGRESS / TARGET IMPORT UNVALIDATED** with `N/A` progress.

Accepted deterministic code-side foundation now includes:

- target-neutral export IR for container/heading/button/image/plain-text/REVIEW intents;
- deterministic local Elementor v0.4 Container/Widget Template JSON candidate generation;
- read-only selected-Figma-Frame extraction for bounded HORIZONTAL/VERTICAL Auto Layout, padding/gap/alignment and visible plain text;
- documented-core `text-editor` mapping with HTML escaping and line-break retention;
- fail-closed REVIEW for manual/grid/wrapped/absolute/image-asset/depth/node/unsupported states;
- sanitized normal + publishable `Preview Elementor` UI with no candidate/template bytes;
- bounded user-declared WordPress/Elementor TargetProfile alignment preview;
- deterministic compatibility categories `NATIVE`, `NATIVE_WITH_REVIEW`, `CONVERTIBLE`, `FALLBACK`, `UNSUPPORTED`, `UNKNOWN`;
- categorical mapping-readiness states `READY`, `READY_WITH_REVIEW`, `NOT_READY`, `INSUFFICIENT_EVIDENCE`;
- coverage denominator retains unknown/unsupported/review-classified nodes instead of silently excluding them.

The readiness surface is mapping readiness only, not observed target compatibility.

Explicit authority remains:

- `referenceClosureStatus=NOT_RUN`;
- `targetEnvironmentValidationStatus=NOT_RUN`;
- `environmentObserved=false`;
- `targetCompatibilityClaim=false`;
- `productionAcceptance=false`;
- `downloadEnabled=false`;
- `importValidationStatus=NOT_RUN`;
- no target observation/network/import/editor/render execution;
- no file/template download or section transfer;
- no Figma mutation.

Next commercial-V1 work is the bounded fresh locally validated Template JSON download contract, then a small deterministic visual-fidelity mapping pack, then one real Elementor import proof. Each step must preserve precise authority labels until genuine evidence exists.

## P16 state

P16 remains **CORE FOUNDATION IN PROGRESS / TARGET VALIDATION UNWIRED** with `N/A` progress.

Retained foundation includes normalized Gutenberg candidate/capability/profile contracts, exact candidate identity, external receipt/offline revalidation, sanitized review/decision prerequisites, genuine-evidence retention requirements, offline operator export/current-manifest validation, and hardened bounded local JSON I/O/output handling with stable snapshots, alias/path checks and fail-closed atomic writes.

This remains supporting evidence. Genuine authenticated evidence and real Gutenberg target/editor/import/render validation remain required before stronger authority. During the focused Elementor V1 window, P16 should remain stable unless a concrete shared blocker appears.

## P17-P27 state

- P17-P26 — **PREFLIGHT FROZEN / IMPLEMENTATION NOT STARTED**;
- P27 — **GATE DEFINED / EXECUTION DEFERRED** under #182.

Do not open these phases merely to increase parallelism while the bounded Elementor V1 release train is unfinished.

## Immediate execution target

Ship a coherent Elementor-first internal V1 path:

`selected Figma Frame -> deterministic extraction -> mapping readiness -> fresh local candidate -> local artifact validation -> explicit Template JSON download -> controlled real Elementor import evidence`

The locally validated artifact must be labeled as local validation only until real target import/editor/render evidence changes that truth.
