name: CI

on:
  pull_request:
  push:
    branches: [main, feat/p5-safe-recipes]

jobs:
  verify:
    runs-on: ubuntu-latest
    env:
      FIGMA_PLUGIN_ID: ${{ vars.FIGMA_PLUGIN_ID || '000000000000000000' }}
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 20
      - run: npm install
      - run: npm run typecheck
      - run: npm test
      - name: Build Figma development plugin
        env:
          SOURCE_SHA: ${{ github.event.pull_request.head.sha || github.sha }}
        run: npm run build
      - name: Verify Figma development bundle
        env:
          EXPECTED_DEV_COMMAND: p5-runtime-self-test
          SOURCE_SHA: ${{ github.event.pull_request.head.sha || github.sha }}
        run: |
          node --input-type=module <<'NODE'
          import { existsSync, readFileSync, statSync } from 'node:fs';
          const manifest = JSON.parse(readFileSync('dist/manifest.json', 'utf8'));
          const required = [manifest.main, manifest.ui, 'verify-p5-evidence.mjs'];
          if (manifest.id !== process.env.FIGMA_PLUGIN_ID) throw new Error(`manifest id ${manifest.id} != configured ${process.env.FIGMA_PLUGIN_ID}`);
          for (const file of required) {
            if (!file || !existsSync(`dist/${file}`) || statSync(`dist/${file}`).size === 0) throw new Error(`missing/empty artifact target: ${file}`);
          }
          if (!Array.isArray(manifest.menu) || !manifest.menu.some((item) => item.command === process.env.EXPECTED_DEV_COMMAND)) throw new Error(`missing expected developer command: ${process.env.EXPECTED_DEV_COMMAND}`);
          const code = readFileSync('dist/code.js', 'utf8');
          for (const [label, value] of [['source sha', process.env.SOURCE_SHA], ['Actions run id', process.env.GITHUB_RUN_ID], ['Actions run number', process.env.GITHUB_RUN_NUMBER]]) {
            if (!value || !code.includes(value)) throw new Error(`compiled plugin missing ${label}: ${value ?? '<missing>'}`);
          }
          console.log(`Verified Figma bundle + compiled provenance: ${manifest.main}, ${manifest.ui}, ${process.env.EXPECTED_DEV_COMMAND}`);
          NODE
      - name: Verify offline P5 evidence CLI
        env:
          SOURCE_SHA: ${{ github.event.pull_request.head.sha || github.sha }}
        run: |
          node --input-type=module <<'NODE' > /tmp/p5-pass.json
          const bundle = {
            schemaVersion: 2,
            capturedAt: '2026-09-08T12:00:01.000Z',
            pluginVersion: '0.1.0-alpha.1',
            runtimeGateVersion: 'p5-runtime-proof-v3',
            build: { sourceSha: process.env.SOURCE_SHA, runId: process.env.GITHUB_RUN_ID, runNumber: process.env.GITHUB_RUN_NUMBER },
            runtimeProofPassedAt: '2026-09-08T12:00:00.000Z',
            acceptance: { accepted: true, failures: [] },
            calibration: {
              schemaVersion: 1,
              passed: true,
              forcedReject: { state: 'REJECTED', validationRejected: true, pixelEvidenceReturned: true, changedPixelPct: 1.5, candidateDeleted: true, originalUntouched: true },
              passRestore: { state: 'COMMITTED', validationPassed: true, pixelEvidenceReturned: true, changedPixelPct: 0, committed: true, restored: true, checkpointCleared: true },
              passFinalize: { state: 'COMMITTED', validationPassed: true, pixelEvidenceReturned: true, changedPixelPct: 0, committed: true, finalized: true, candidateRetained: true, originalDiscarded: true, checkpointCleared: true },
              leftovers: 0,
            },
          };
          console.log(JSON.stringify(bundle));
          NODE
          node dist/verify-p5-evidence.mjs < /tmp/p5-pass.json | grep '"accepted": true'
          if printf '{}\n' | node dist/verify-p5-evidence.mjs > /tmp/p5-invalid.out 2>&1; then
            echo 'offline verifier unexpectedly accepted malformed evidence'
            exit 1
          fi
          grep '"accepted": false' /tmp/p5-invalid.out
          node --input-type=module <<'NODE' > /tmp/p5-old-build.json
          const bundle = JSON.parse(await (await import('node:fs/promises')).readFile('/tmp/p5-pass.json', 'utf8'));
          bundle.build.sourceSha = '0123456789abcdef0123456789abcdef01234567';
          console.log(JSON.stringify(bundle));
          NODE
          if node dist/verify-p5-evidence.mjs < /tmp/p5-old-build.json > /tmp/p5-old-build.out 2>&1; then
            echo 'offline verifier unexpectedly accepted different-build evidence'
            exit 1
          fi
          grep 'different build than this offline verifier artifact' /tmp/p5-old-build.out
      - name: Verify deterministic local-import preparation
        run: |
          npm run prepare:figma-import -- 123456789012345678 dist dist-local
          node --input-type=module <<'NODE'
          import { createHash } from 'node:crypto';
          import { readFileSync } from 'node:fs';
          const hash = (path) => createHash('sha256').update(readFileSync(path)).digest('hex');
          const manifest = JSON.parse(readFileSync('dist-local/manifest.json', 'utf8'));
          if (manifest.id !== '123456789012345678') throw new Error('prepared manifest did not bind requested local plugin id');
          for (const file of [manifest.main, manifest.ui]) {
            if (hash(`dist/${file}`) !== hash(`dist-local/${file}`)) throw new Error(`local import preparation changed compiled target ${file}`);
          }
          if (!readFileSync('dist-local/LOCAL_IMPORT_INFO.txt', 'utf8').includes('compiled_targets_unchanged=true')) throw new Error('local import proof note is missing unchanged-target assertion');
          console.log('Verified manifest-only local Figma import preparation.');
          NODE
      - name: Package standalone local-import helper
        run: |
          cp scripts/prepare-figma-import.mjs dist/prepare-figma-import.mjs
          test -s dist/prepare-figma-import.mjs
      - name: Write Figma build provenance
        env:
          SOURCE_SHA: ${{ github.event.pull_request.head.sha || github.sha }}
        run: |
          printf 'source_sha=%s\nworkflow_sha=%s\nhead_ref=%s\nbase_ref=%s\nrun_id=%s\nrun_number=%s\nfigma_plugin_id=%s\n' \
            "$SOURCE_SHA" "$GITHUB_SHA" "$GITHUB_HEAD_REF" "$GITHUB_BASE_REF" "$GITHUB_RUN_ID" "$GITHUB_RUN_NUMBER" "$FIGMA_PLUGIN_ID" \
            > dist/BUILD_INFO.txt
          if [ "$FIGMA_PLUGIN_ID" = "000000000000000000" ]; then
            cat > dist/IMPORT_NOTES.txt <<'EOF'
          This build used the repository's placeholder Figma plugin id (000000000000000000).
          After unpacking this artifact, run:
            node prepare-figma-import.mjs <your-figma-plugin-id> . dist-local
          Then import dist-local/manifest.json in Figma. The helper preserves compiled code/UI byte-for-byte and changes only manifest.json id.
          Exported P5 closure JSON from this exact artifact can be independently checked with:
            node verify-p5-evidence.mjs < p5-evidence.json
          The verifier rejects evidence captured by another artifact build. Do not treat CI success, manifest preparation, or offline verification as imported-Figma runtime proof.
          EOF
          else
            printf 'This build used configured Figma plugin id %s. Exported P5 evidence from this exact artifact can be checked with node verify-p5-evidence.mjs < p5-evidence.json. CI success is not imported-Figma runtime proof.\n' "$FIGMA_PLUGIN_ID" > dist/IMPORT_NOTES.txt
          fi
      - name: Upload Figma development build
        uses: actions/upload-artifact@v4
        with:
          name: figma-plugin-dist-${{ github.run_number }}
          path: dist/
          if-no-files-found: error
          retention-days: 14
