name: P14 Receipt Diagnostic Wiring

on:
  push:
    branches:
      - p14/receipt-diagnostic-evidence

permissions:
  contents: write

jobs:
  wire:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
        with:
          ref: p14/receipt-diagnostic-evidence
          fetch-depth: 0
      - uses: actions/setup-node@v7
        with:
          node-version: 20
      - name: Verify exact branch scope
        run: |
          set -euo pipefail
          test "$(git branch --show-current)" = "p14/receipt-diagnostic-evidence"
          test -f scripts/p14-receipt-diagnostic-wiring.py
          test -f .github/workflows/p14-receipt-diagnostic-wiring.yml
      - name: Apply exact receipt diagnostic wiring
        run: python scripts/p14-receipt-diagnostic-wiring.py
      - name: Install dependencies
        run: npm install
      - name: Verify P14 receipt diagnostic contracts
        run: |
          set -euo pipefail
          npm run typecheck
          npx vitest run tests/p14-*.test.ts
      - name: Commit verified product wiring
        run: |
          set -euo pipefail
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add \
            src/core/p14-retained-duplicate-transaction.ts \
            src/core/p14-preparation-receipt.ts
          git diff --cached --quiet && { echo "No product wiring changes staged"; exit 1; }
          git commit -m "feat: bound P14 receipt diagnostic evidence"
          git push origin HEAD:p14/receipt-diagnostic-evidence
