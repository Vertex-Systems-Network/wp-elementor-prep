name: P14 post-236 status sync

on: push

permissions:
  contents: write

jobs:
  sync:
    if: github.ref == 'refs/heads/docs/p14-post-236-status-237'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - name: Apply canonical status synchronization
        run: python .github/scripts/p14-status-sync-237.py
